import styles from "@/styles/components/preloader/circularDot.module.scss";

// The most complicated file in the entire project /s
export default function CircularDot() {
  return <span className={styles.circularDot}></span>;
}
